import styles from "@/components/footer/Footer.module.css";

const Footer = (): JSX.Element => {
  return (
    <footer className={styles.footer}>
      <h1>Made with love by Jorge Agoiz</h1>
    </footer>
  );
};

export default Footer;
